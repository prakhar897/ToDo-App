A ToDo App made with Django and Python.

To run the app:
->  install python
->  pip3 install pipenv
->  pipenv install django
->  pipenv shell
->  django-admin startproject test_project .
->  python manage.py runserver